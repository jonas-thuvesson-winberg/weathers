---
import WeatherEntryBody from "./WeatherEntryBody.astro";

const { date } = Astro.props as {
  date: string;
};
---

<h1>{date}</h1>
<div class={`flex justify-end pr-2 pb-2 border-b-1 border-gray-300`}></div>
<slot />
