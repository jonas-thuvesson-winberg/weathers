import Pipeable from "./_pipeable";
import PipeableAsync from "./_pipeable-async";

const pipeable = Pipeable.from;
const pipeableAsync = PipeableAsync.from;

export { Pipeable, PipeableAsync, pipeable, pipeableAsync };
